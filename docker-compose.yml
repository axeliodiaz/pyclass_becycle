services:
  web_scraping:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    command: python single_version.py
    environment:
      - PYTHONUNBUFFERED=1
